# PDFs

Lege hier dein eigenes Regel-PDF ab, z. B. `pdfs/mein-spiel.pdf`.

Aus Urheberrechtsgruenden sind in diesem Repo **keine Regelhefte** enthalten
(`pdfs/*.pdf` ist in `.gitignore`). Alle Skripte indizieren automatisch jedes
PDF, das in diesem Ordner liegt.
